#!/usr/bin/env node

var Lexer = require('../lexer.js');

var lexer = new Lexer();
process.stdin.setEncoding('utf8');
process.stdin.on('data', function (str) {
  console.log(lexer.parse(str));
});
process.stdin.on('end', function () {
  console.log(lexer.end());
});
